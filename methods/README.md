# Locked methods and amendments

The screening codebook and model-review protocol describe the operational selection rules. `FRESH_ANALYSIS_SPECIFICATION_V2_2026-08-14.md` is the base analysis specification; it must be read together with `AMENDMENT_006_FINAL_INTEGRATION.json`, which is the controlling final amendment for the six-source 547-publication/485-study package.

Earlier or intermediate counts in the base specification are historical inputs to the amendment chain and are not the final result authority. Final public counts are fixed by `audit/PUBLIC_RELEASE_FREEZE_V2.json`, `audit/COMBINED_ANALYSIS_SUMMARY_V5.json`, `RELEASE_MANIFEST.json`, and `SHA256SUMS.csv`.
